# Nextest configuration for canopy
# https://nexte.st/docs/configuration/

[profile.default]
# Run all tests by default
default-filter = "all()"

[profile.ci]
# CI profile - same as default
default-filter = "all()"

[profile.full]
# Full profile runs everything
default-filter = "all()"
